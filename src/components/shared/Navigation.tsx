import { ProfileMenu } from "./ProfileMenu";

const Navigation = () => {
  return (
    <div className="flex items-center gap-4">
      {/* Other nav items */}
      <ProfileMenu />
    </div>
  );
};

export default Navigation; 